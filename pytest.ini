[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --tb=short -v
# 集成测试默认跳过（需要 -m integration 显式运行）
markers =
    integration: 依赖完整数据库或外部 API 的集成测试
